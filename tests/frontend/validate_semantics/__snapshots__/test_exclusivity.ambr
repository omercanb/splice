# serializer version: 1
# name: test_aliasing_errors
  '''
  program_aliasing_errors.py:6:5: error: `items` and `items` could alias, and aliasing is not permitted for mutable parameters
  
    6 |     mutate(items, items)
      |     ^^^^^^^^^^^^^^^^^^^^
  
    help: pass a copy so they can't alias:
            mutate(items, copy(items))
  
  program_aliasing_errors.py:10:5: error: `matrix[0]` and `matrix[0]` could alias, and aliasing is not permitted for mutable parameters
  
    10 |     mutate(matrix[0], matrix[0])
       |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  
    help: pass a copy so they can't alias:
            mutate(matrix[0], copy(matrix[0]))
  
  program_aliasing_errors.py:14:5: error: `items` and `items` could alias, and aliasing is not permitted for mutable parameters
  
    14 |     items.extend(items)
       |     ^^^^^^^^^^^^^^^^^^^
  
    help: pass a copy so they can't alias:
            items.extend(copy(items))
  
  program_aliasing_errors.py:18:5: error: `items` and `items[0]` could alias, and aliasing is not permitted for mutable parameters
  
    18 |     items.append(items[0])
       |     ^^^^^^^^^^^^^^^^^^^^^^
  
    help: pass a copy so they can't alias:
            items.append(copy(items[0]))
  
  program_aliasing_errors.py:22:5: error: `items` and `items` could alias, and aliasing is not permitted for mutable parameters
  
    22 |     items += items
       |     ^^^^^^^^^^^^^^
  
    help: pass a copy so they can't alias:
            items += copy(items)
  
  program_aliasing_errors.py:26:5: error: `matrix[0]` and `matrix[0]` could alias, and aliasing is not permitted for mutable parameters
  
    26 |     matrix[0] += matrix[0]
       |     ^^^^^^^^^^^^^^^^^^^^^^
  
    help: pass a copy so they can't alias:
            matrix[0] += copy(matrix[0])
  
  program_aliasing_errors.py:31:9: error: `items` could alias `items`, which this loop is iterating
  
    31 |         items.append(1)
       |         ^^^^^
  
    help: iterate a copy so mutating it inside the loop can't alias:
            copy(items)
  
  program_aliasing_errors.py:36:9: error: `items` could alias `items`, which this loop is iterating
  
    36 |         items.append(1)
       |         ^^^^^
  
    help: iterate a copy so mutating it inside the loop can't alias:
            enumerate(copy(items))
  
  program_aliasing_errors.py:41:9: error: `b` could alias `b`, which this loop is iterating
  
    41 |         b.append(1)
       |         ^
  
    help: iterate a copy so mutating it inside the loop can't alias:
            zip(a, copy(b))
  
  '''
# ---
